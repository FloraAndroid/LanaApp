<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.archeticture.flora.lanaapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ItemDetailListActivity"
            android:label="@string/title_itemdetail_list"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".ItemDetailDetailActivity"
            android:label="@string/title_itemdetail_detail"
            android:parentActivityName=".ItemDetailListActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.archeticture.flora.lanaapp.ItemDetailListActivity" />
        </activity>
        <activity android:name=".motion.MotionListActvity" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/SplashCold">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>